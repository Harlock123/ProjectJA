{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "App": {
    "//PublicBaseUrl": "Optional public-facing origin (e.g. https://projectja.example.com) baked into outbound links — invite emails, password-reset emails, copy-to-clipboard. Leave empty to use the inbound request's Host. Env override: App__PublicBaseUrl",
    "PublicBaseUrl": ""
  }
}
